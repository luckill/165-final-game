package tage.cameraController;

/**
 * enum for related camera movement
 */
public enum CameraMovementType
{
    ZOOM_IN, ZOOM_OUT, PITCH_UP, PITCH_DOWN, ROTATE_LEFT, ROTATE_RIGHT, PAN_LEFT, PAN_RIGHT, PAN_FORWARD, PAN_BACKWARD
}